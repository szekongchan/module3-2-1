# Copy this to terraform.tfvars for local usage.
# Do not commit terraform.tfvars with real environment values.

log_bucket_name    = "your-existing-log-bucket-name"
replica_bucket_arn = "arn:aws:s3:::your-existing-replica-bucket"
